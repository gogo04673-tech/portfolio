import 'package:flutter/material.dart';

class MyWorksTablet extends StatelessWidget {
  const MyWorksTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
